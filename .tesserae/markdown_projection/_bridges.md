# Cross-vault bridges

> Every `wiki://<alias>/<kind>/<slug>` URI mentioned across this vault, grouped by destination alias. Update the registry with `tesserae projects register` to make any of these resolvable.

_No outbound cross-vault references in this vault._
