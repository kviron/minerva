{
  "title": "0005-mcp-oauth-resource-and-revocation.md",
  "kind": "raw",
  "body_text": "# ADR 0005: Bind MCP tokens to one resource and enforce immediate revocation\n\nDate: 2026-06-24  \nStatus: proposed\n\n## Context\n\nMinerva exposes private project documentation over remote Streamable HTTP MCP. OAuth scopes limit the delegated client and project RBAC limits the user, but neither protects the resource server if a token is accepted for the wrong audience or remains usable after its grant is revoked.\n\nThe first-release acceptance criteria require the next MCP request after revocation to fail.\n\n## Decision\n\n- Define one canonical URI for the Minerva MCP protected resource and publish it through OAuth Protected Resource Metadata.\n- Require OAuth authorization and token requests to bind issued tokens to that resource.\n- Validate token activity, issuer, canonical resource/audience, scopes, and current RBAC on every MCP request before tool dispatch.\n- Represent MCP access tokens in a form whose active state can be checked server-side on every request.\n- Revoke a grant atomically with all access and refresh tokens derived from that grant.\n- Return `401 Unauthorized` for the first request using a revoked or otherwise inactive token.\n- Record grant revocation and rejected post-revocation requests in the audit trail without storing raw tokens.\n\n## Consequences\n\n- Purely offline acceptance of long-lived self-contained access tokens is not sufficient for the first release.\n- Token validation adds a server-side lookup or equivalent active-state check to each MCP request.\n- Revocation tests must cover consent, access tokens, refresh tokens, resource mismatch, scope enforcement, and current RBAC.\n- The exact canonical production URI is deployment configuration, but normalization and comparison rules are fixed before Slice 6 implementation.",
  "source_path": "D://develop//minerva//docs//decisions//0005-mcp-oauth-resource-and-revocation.md",
  "links": [],
  "body": "# ADR 0005: Bind MCP tokens to one resource and enforce immediate revocation\n\nDate: 2026-06-24  \nStatus: proposed\n\n## Context\n\nMinerva exposes private project documentation over remote Streamable HTTP MCP. OAuth scopes limit the delegated client and project RBAC limits the user, but neither protects the resource server if a token is accepted for the wrong audience or remains usable after its grant is revoked.\n\nThe first-release acceptance criteria require the next MCP request after revocation to fail.\n\n## Decision\n\n- Define one canonical URI for the Minerva MCP protected resource and publish it through OAuth Protected Resource Metadata.\n- Require OAuth authorization and token requests to bind issued tokens to that resource.\n- Validate token activity, issuer, canonical resource/audience, scopes, and current RBAC on every MCP request before tool dispatch.\n- Represent MCP access tokens in a form whose active state can be checked server-side on every request.\n- Revoke a grant atomically with all access and refresh tokens derived from that grant.\n- Return `401 Unauthorized` for the first request using a revoked or otherwise inactive token.\n- Record grant revocation and rejected post-revocation requests in the audit trail without storing raw tokens.\n\n## Consequences\n\n- Purely offline acceptance of long-lived self-contained access tokens is not sufficient for the first release.\n- Token validation adds a server-side lookup or equivalent active-state check to each MCP request.\n- Revocation tests must cover consent, access tokens, refresh tokens, resource mismatch, scope enforcement, and current RBAC.\n- The exact canonical production URI is deployment configuration, but normalization and comparison rules are fixed before Slice 6 implementation.\n"
}
